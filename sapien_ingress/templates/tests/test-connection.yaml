apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sapien_ingress.fullname" . }}-test-connection"
  labels:
    {{- include "sapien_ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sapien_ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
