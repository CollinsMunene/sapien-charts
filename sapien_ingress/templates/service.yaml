apiVersion: v1
kind: Service
metadata:
  name: {{ include "sapien_ingress.fullname" . }}
  labels:
    {{- include "sapien_ingress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sapien_ingress.selectorLabels" . | nindent 4 }}
